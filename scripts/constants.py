STACK_MAX = 30  # Normalization base for stack size
SUCCESSOR_MAX = 2  # Max successor count (exact: 0=STOP, 1=JUMP, 2=JUMPI)
TEST_CASE_NUMBER_MAX = 50  # Normalization base for test case count
# branch_new_instruction and path_new_instruction use real_bytecode_len as base
DEPTH_MAX = 15  # Normalization base for fork depth
# cpicnt and covNew use real_bytecode_len as base
ICNT_MAX = 10  # Normalization base for instruction visit count
SUBPATH_MAX = 5  # Normalization base for subpath visit count
REWARD_MAX = 100  # Normalization base for reward
